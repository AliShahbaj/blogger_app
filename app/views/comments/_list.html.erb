<% if comments.count > 0 %>
  <h3>Comments</h3>
  <% comments.each do |comment| %>
    <p><span><%= comment.body %></span></p>
    <em><%= time_ago_in_words(comment.created_at) %>: <%= comment.post.user.email %></em>
    <hr>
  <% end %>

<% end %>